# ============================================================
# .env.production — Production Configuration for Docker
# ============================================================

VITE_API_MODE=real
# Prefix all requests with /ocsc-circular to support IIS routing
VITE_API_BASE_URL="/ocsc-circular"
