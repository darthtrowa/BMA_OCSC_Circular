# ============================================================
# .env.production — Production Configuration for Docker
# ============================================================

VITE_API_MODE=real
# Empty string forces relative paths over the Nginx proxy (port 80)
VITE_API_BASE_URL=""
