# ============================================================
# .env.production — Production Configuration for Docker
# ============================================================

VITE_API_MODE=real
# Prefix all requests with /bma_ocsc_circular to support IIS routing
VITE_API_BASE_URL="/bma_ocsc_circular"
