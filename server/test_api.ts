import axios from 'axios';
import https from 'https';

async function test() {
  // Can't easily test without auth token...
  // Wait, I can run an express mock or just check `server/src/routes/admin.ts`
  process.exit(0);
}
test();
